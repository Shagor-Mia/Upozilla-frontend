/** Civic Horizon input pattern (DESIGN.md "Input Fields"): 2px muted border that
 * shifts to primary green on focus. Shared by every form so they match. */
export const inputClass =
  "text-body-md w-full rounded-xl border-2 border-border-muted bg-surface px-4 py-2.5 text-on-surface transition-colors focus:border-primary focus:outline-none focus:ring-0 disabled:opacity-60";

export const selectClass = inputClass;

export const labelClass = "text-label-sm text-on-surface";
